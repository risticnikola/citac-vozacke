# Default: normalize to LF on checkin, use LF on checkout (cross-platform safety)
* text=auto eol=lf

# Shell scripts must always be LF
*.sh text eol=lf

# Windows-only scripts stay CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
